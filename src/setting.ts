// import _ = require('lodash')
export default class Setting {
  environment        = "unknown"
  root_endpoint      = ""

  constructor(json: {}) {
    // _.each(json, (value, key) => {
      // if (_.has(this, key)) {
        // this[key] = value
      // }
    // })
  }
}
